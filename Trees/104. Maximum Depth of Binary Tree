class Solution {
public:
    int dfs(TreeNode* r) {
        if(!r) return 0;

        int left=dfs(r->left);
        int right=dfs(r->right);
        return 1+max(left,right);
    }
    int maxDepth(TreeNode* root) {
        return dfs(root);
    }
};
